{
  "_comment": "Repos whose per-repository GitHub Security Advisories we ingest (github_repo source). For non-ecosystem software (desktop apps, firmware, standalone binaries) whose advisories never reach the global GHSA feed. Package software does NOT belong here — it comes in via the `ghsa` source. Format: owner/repo.",
  "repos": [
    "notepad-plus-plus/notepad-plus-plus"
  ]
}
